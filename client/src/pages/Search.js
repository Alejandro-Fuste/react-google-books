import React from 'react';
import SearchForm from '../components/SearchForm';

const Search = (props) => {
	return (
		<div>
			<SearchForm />
			<h1>Search Page</h1>
		</div>
	);
};

export default Search;
